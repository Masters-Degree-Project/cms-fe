version: '3'
services:
  app:
    container_name: app
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    ports:
      - 3000:3000
    networks:
      - v3
    dns:
      - 1.1.1.1
      - 8.8.8.8

networks:
  v3:
    external: true
